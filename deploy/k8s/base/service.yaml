apiVersion: v1
kind: Service
metadata:
  name: dynamic-pricing-api-service
spec:
  selector:
    app: dynamic-pricing-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP